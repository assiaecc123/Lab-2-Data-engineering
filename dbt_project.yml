name: 'playstore_pipeline'
version: '1.0.0'

profile: 'playstore_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  playstore_pipeline:
    staging:                      # lowercase to match folder name on disk
      +materialized: view
    marts:
      +materialized: table